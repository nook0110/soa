services:
  product-service:
    build: ./product-service
    container_name: product-service
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
    networks:
      - marketplace-network

networks:
  marketplace-network:
    driver: bridge